# 第 13 章 完整实战：从 0 到 1 训练并部署你自己的 MiniMind

> 本章是全教程的综合实验：把第 3~12 章的所有知识串成一条完整流水线，从环境准备到最终部署，给出每一步的可执行命令、预期结果与排错指引。
>
> 建议：本章按顺序逐节执行，不要跳步。完整跑完大约需要：CPU 数小时～一天；单卡 3090 约 2~4 小时 + 约 3 元电费/租金。

---

## 13.1 实战总览

```text
第 0 步：环境与数据准备
第 1 步：Tokenizer 验证（用现成的，不重训）
第 2 步：预训练 Pretrain
第 3 步：SFT 指令微调
第 4 步：质量评估（主观 + 客观）
第 5 步：部署（CLI / API / WebUI）
第 6 步（可选）：LoRA / DPO / RL 进阶
第 7 步：导出与生态对接
```

## 13.2 第 0 步：环境与数据准备

```bash
# 环境（若第 1 章已完成可跳过）
conda create -n minimind python=3.10 -y && conda activate minimind
cd /path/to/minimind
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple

# 验证
python -c "import torch; print('CUDA:', torch.cuda.is_available())"

# 下载数据（快速复现只需两个 mini 文件）
pip install modelscope
modelscope download --dataset gongjy/minimind_dataset pretrain_t2t_mini.jsonl --local_dir ./dataset
modelscope download --dataset gongjy/minimind_dataset sft_t2t_mini.jsonl --local_dir ./dataset
# 若 CLI 下载失败，也可从 ModelScope 数据集页面手动下载同名文件放入 ./dataset/
# https://www.modelscope.cn/datasets/gongjy/minimind_dataset/files

# 验证数据
ls -lh ./dataset/           # pretrain_t2t_mini.jsonl (~1.2GB), sft_t2t_mini.jsonl (~1.6GB)
head -c 500 ./dataset/pretrain_t2t_mini.jsonl
```

> ⚠️ 硬件对照：24GB 显存（3090/4090）可全速跑默认配置；8GB 显存建议 `--batch_size 8 --max_seq_len 512`；纯 CPU 也能跑通（速度慢 20~50 倍，建议把 `--epochs` 调为 1）。

## 13.3 第 1 步：Tokenizer 验证

```bash
python -c "
from transformers import AutoTokenizer
tok = AutoTokenizer.from_pretrained('./model')
print('vocab:', len(tok), '| bos:', tok.bos_token_id, '| eos:', tok.eos_token_id)
print(tok.encode('你好，世界'))
"
```

**检查点**：输出 `vocab: 6400` 且编码结果以 `1` 开头、`2` 结尾。

## 13.4 第 2 步：预训练

```bash
cd trainer
python train_pretrain.py --max_seq_len 768
```

**检查点**（对照第 5 章）：

1. `Model Params: 63.91M` 打印正常；
2. 首个日志 loss ≈ 8.7（随机初始化锚点）；
3. 训练中 loss 单调下降，无 NaN；
4. 结束时有 `out/pretrain_768.pth`（fp16 权重）。

**排错速查**：loss 不动 → 检查数据文件是否为空/格式错；OOM → 降 `--batch_size`/`--max_seq_len`；NaN → 换 bf16 或降 lr。

## 13.5 第 3 步：SFT

```bash
cd trainer
python train_full_sft.py --max_seq_len 768
```

**检查点**：

1. 日志开头显示基于 `pretrain_768.pth` 加载（loss 起点 ≈1.x 而非 8.7）；
2. 训练结束得到 `out/full_sft_768.pth`。

**验证对话能力**：

```bash
cd ..
python eval_llm.py --weight full_sft
```

输入 `[1] 手动输入` 后提问，例如"解释什么是机器学习"。此时你已拥有一个**从 0 训练出来的可对话模型**——这就是 MiniMind "Zero" 路线。

## 13.6 第 4 步：质量评估

### 4.1 主观评估（CLI 多轮）

```bash
python eval_llm.py --weight full_sft --historys 4          # 带 4 轮记忆
python eval_llm.py --weight full_sft --temperature 0.3     # 保守模式
python eval_llm.py --weight full_sft --open_thinking 1     # 思考模式
```

### 4.2 客观评估（lm-eval-harness，可选）

```bash
# 安装 lm-evaluation-harness
git clone https://github.com/EleutherAI/lm-evaluation-harness
cd lm-evaluation-harness && pip install -e .

# 评估（需先导出 transformers 格式模型，见 13.8）
# 指令模型需要 --apply_chat_template；数据集可选：
#   ceval-valid / cmmlu / arc_easy / piqa / openbookqa / hellaswag / social_iqa
HF_ENDPOINT=https://hf-mirror.com lm_eval --model hf \
    --model_args pretrained="/path/to/model",dtype=auto \
    --tasks "ceval-valid" --batch_size 16 --device cpu \
    --trust_remote_code --apply_chat_template
```

**参考数据**（README 实测，minimind-3 64M）：

| 数据集 | C-Eval | CMMLU | ARC-Easy | PIQA | OpenBookQA | HellaSwag | SIQA |
|--------|--------|-------|----------|------|------------|-----------|------|
| minimind-3 | 24.89 | 25.38 | 28.49 | 50.65 | 23.60 | 28.28 | 34.19 |

> 解读（README 原话的要点）：选择题评测是比较各候选项的条件概率，随机作答准确率本身就是很强的下界，这个量级的模型确实长期徘徊在附近。64M 模型分数不会高——**评估的目的是建立"小模型能力边界"的体感**，不是刷分。有趣的是，README 还演示了只做"评测格式对齐"（lora_exam 数据微调）即可平均提升约 2.9 个百分点，说明这类任务上小模型的瓶颈未必全在知识，也可能在输入格式。
>
> ⚠️ 注意数据污染：不要在评测集上训练（README 演示过污染后可跑到 97%，但毫无意义）。

### 4.3 过程可视化（可选）

```bash
python train_pretrain.py --use_wandb     # 或任何训练脚本加 --use_wandb
```

SwanLab/WandB 曲线：loss、learning_rate、epoch_time。

## 13.7 第 5 步：部署三件套

### 5.1 CLI（已完成，见 13.5）

### 5.2 OpenAI 兼容 API

```bash
# 方式 A：transformers 格式直接服务
python scripts/serve_openai_api.py --load_from ./out_model

# 方式 B：原生权重（在 scripts/ 目录下运行，从 out/ 加载）
cd scripts && python serve_openai_api.py --weight full_sft
```

测试（服务监听 8998 端口）：

```python
from openai import OpenAI
client = OpenAI(base_url="http://localhost:8998/v1", api_key="x")
r = client.chat.completions.create(model="minimind",
    messages=[{"role": "user", "content": "你好"}])
print(r.choices[0].message.content)
```

### 5.3 Streamlit WebUI

```bash
# web_demo 扫描 scripts/ 下含权重的 transformers 模型目录
cp -r ./out_model ./scripts/out_model
cd scripts && streamlit run web_demo.py
```

浏览器打开本地地址即可对话，支持思考展示与工具选择。

## 13.8 第 6 步（进阶）：LoRA / DPO / RL 实战组合

按数据与算力选择（全部基于 `full_sft_768.pth`）：

| 目标 | 脚本 | 数据 | 备注 |
|------|------|------|------|
| 垂域适配 | `train_lora.py` | 自制 `lora_xxx.jsonl`（几十~几百条） | CPU 可跑，10 分钟级 |
| 偏好对齐 | `train_dpo.py` | `dpo.jsonl`（53MB） | 需 4e-8 低 lr |
| 推理强化 | `train_grpo.py` | `rlaif.jsonl`（24MB） | 需外部 RM |
| 工具智能 | `train_agent.py` | `agent_rl.jsonl`（86MB） | 需外部 RM |
| 压缩提效 | `train_distillation.py` | 复用 SFT 数据 | MoE 教师 → Dense 学生 |

**推荐的完整进阶路线**（每步都在上一步基础上）：

```bash
cd trainer
python train_lora.py --lora_name lora_identity --data_path ../dataset/lora_identity.jsonl   # ① 自我认知
python train_dpo.py                                                                          # ② 偏好对齐
python train_grpo.py --debug_mode                                                           # ③ 强化学习
python eval_llm.py --weight grpo            # 最终模型测试
```

## 13.9 第 7 步：导出与生态对接

### 7.1 原生权重 → Transformers 格式

```bash
# scripts/convert_model.py 提供 torch ⇄ transformers 互转（__main__ 中可配置路径）
cd scripts && python convert_model.py
```

> 转换逻辑（`convert_torch2transformers`）：`MiniMindForCausalLM` 实例化 + `load_state_dict` + `save_pretrained`，并把 tokenizer 一并保存为 transformers 格式目录（供 `serve_openai_api.py --load_from`、lm-eval、llama.cpp 等使用）。

### 7.2 LoRA 合并导出

```bash
# convert_model.py 中的 convert_merge_base_lora：基模 + LoRA → 完整权重
```

### 7.3 第三方推理引擎（README 详细步骤）

```bash
# llama.cpp：转换 GGUF 后可极低资源运行
python scripts/convert_gguf.py ...        # 按 README 指引
# ollama
ollama create minimind -f Modelfile
ollama run minimind
# vllm
vllm serve /path/to/model --served-model-name "minimind"
```

> 生态对接的价值：训练一次（PyTorch），到处运行（llama.cpp 手机端 / ollama 桌面 / vllm 服务端）。

## 13.10 长上下文：YaRN 外推

```bash
python eval_llm.py --weight full_sft --max_new_tokens 4000 --inference_rope_scaling
```

原理回顾（第 7 章 7.4）：低频维度频率拉伸，缓解超长位置编码断裂。训练长度 768 的模型，外推到 2048~3072 通常可用；超过 4 倍后质量明显退化。

## 13.11 完整训练时间与成本对照（单卡 3090 实测参考）

| 阶段 | 数据 | 耗时 | 成本 |
|------|------|------|------|
| Pretrain (mini) | 1.2GB | ~1.2 h/epoch | ~1.6 元 |
| SFT (mini) | 1.6GB | ~1.1 h/epoch | ~1.4 元 |
| LoRA | 自制小数据 | ~10 min | ~0.2 元 |
| DPO | 53MB | ~30 min | ~0.7 元 |
| GRPO | 24MB | ~1.1 h | ~1.4 元 |

**合计（Zero 路线）≈ 2.3 小时、3 元人民币**——这就是 MiniMind 标题的由来。

## 13.12 实战检查清单

执行完本章后，逐项打勾确认：

- [ ] 环境：`torch.cuda.is_available()` = True（或明确知道在用 CPU）
- [ ] Tokenizer：vocab=6400，bos=1，eos=2
- [ ] Pretrain：loss 从 ~8.7 降至 5 以下，得到 `pretrain_768.pth`
- [ ] SFT：loss 从 ~1.x 降至 ~0.7，得到 `full_sft_768.pth`
- [ ] 对话：`eval_llm.py --weight full_sft` 能完成一问一答
- [ ] 部署：至少跑通 CLI + 一种服务形态（API/WebUI）
- [ ] （进阶）LoRA 至少完成一次，观察到 `LoRA 参数量 ≈ 2.5%`
- [ ] （进阶）对比 base vs LoRA vs DPO vs RL 各阶段回答差异

## 13.13 本章小结

- 完整流水线 = 数据准备 → Tokenizer → Pretrain → SFT → 评估 → 部署；
- 每个阶段的"检查点"（loss 锚点、文件产物、验证命令）是排错的关键；
- Zero 路线 2 小时 3 元可复现；进阶路线（LoRA/DPO/RL/蒸馏）按需叠加；
- 导出格式打通 llama.cpp/ollama/vllm 生态，一次训练到处部署；
- 你现在拥有的能力：读懂并复现一个 LLM 的全部核心训练代码——这已经超越了大多数"调包"使用者。

## 13.14 思考题（综合）

1. 完整复述一条数据从 `jsonl` 到 loss 的流动路径，标注每一步的文件与函数。
2. 如果只给 8GB 显存，你会如何调整整条流水线的每个参数？列出一份完整配置。
3. Pretrain 数据换成纯英文后，SFT 用中英混合，最终模型的表现会如何？（设计一个 30 分钟的小实验验证）
4. 你训练的模型在 C-Eval 上能拿多少分？与随机基线（25%）对比说明了什么？
5. 尝试给模型设计一个"角色设定"的 system prompt，观察 SFT 模型对 system 的服从程度，并用第 3 章的数据增强逻辑解释原因。
